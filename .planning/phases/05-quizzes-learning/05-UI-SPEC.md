---
phase: 5
slug: quizzes-learning
status: draft
shadcn_initialized: true
preset: default (slate base)
created: 2026-03-19
---

# Phase 5 — UI Design Contract

> Visual and interaction contract for the Quizzes & Learning phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (default style, CSS variables enabled) |
| Preset | default / slate base |
| Component library | Radix UI (via shadcn) |
| Icon library | lucide-react ^0.462.0 |
| Font | Inter (sans), Literata (serif, reader only) |

Existing shadcn components reused this phase: `Tabs`, `Button`, `Progress`, `RadioGroup`, `Input`, `Textarea`, `Carousel`, `Card`.

No new shadcn components need to be added.

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gaps, inline padding |
| sm | 8px | Compact element spacing, option gaps |
| md | 16px | Default element spacing, card padding |
| lg | 24px | Section padding, card internal padding |
| xl | 32px | Layout gaps between sections |
| 2xl | 48px | Page top/bottom padding |
| 3xl | 64px | Not used this phase |

Exceptions:
- Flashcard height: 256px (h-64) -- fixed card height for consistent flip animation
- Stat card min-width: 100px -- ensures horizontal scroll items stay readable
- Progress indicator dot: 12px diameter (w-3 h-3)

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 14px (text-sm) | 400 (normal) | 1.625 (leading-relaxed) | Flashcard back text, answer option text, quiz review body |
| Label | 16px (text-base) | 600 (semibold) | 1.375 (leading-snug) | Flashcard front text, option labels, progress labels |
| Heading | 20px (text-xl) | 700 (bold) | 1.2 | Learning page title, results score display |
| Display | 28px (text-2xl) | 800 (extrabold) | 1.1 | Score number on results screen (e.g. "4/5") |

Font family: Inter (sans) for all quiz/learning UI. Literata (serif) is reader-only.

---

## Color

| Role | Token | HSL | Usage |
|------|-------|-----|-------|
| Dominant (60%) | `background` | 38 60% 97% | Page background, quiz question area |
| Secondary (30%) | `card` | 38 50% 99% | Flashcard surfaces, answer option cards, stat cards |
| Accent (10%) | `primary` | 350 78% 21% (burgundy) | "Take Quiz" CTA button, active progress dot, selected answer border, results score |
| Sage | `accent` | 157 18% 62% | Flashcard back gradient, "Knew it" button, mastered indicator, progress bar fill |
| Correct | custom | 142 71% 45% (hsl) | Correct answer highlight background (green-500/15 tint) |
| Wrong | `destructive` | 0 72% 51% | Wrong answer highlight background (red-500/15 tint), "Didn't know" button |

Accent reserved for:
- "Take Quiz" / "Start" primary CTA buttons (burgundy bg)
- Active/current progress indicator dot
- Selected answer option border ring
- Score display on results screen
- Progress bar track fill uses sage green

### Correct/Wrong Answer Feedback Colors

| State | Background | Border | Icon |
|-------|-----------|--------|------|
| Correct (selected) | bg-green-50 (hsl 142 76% 95%) | border-green-500 | Check icon in green-600 |
| Correct (not selected) | bg-green-50/50 | border-green-300 | Check icon in green-500 |
| Wrong (selected) | bg-red-50 (hsl 0 86% 97%) | border-red-500 | X icon in red-600 |
| Neutral (unselected, during feedback) | bg-card | border-border | None |

Dark mode: use green-500/15 and red-500/15 as background tints instead of green-50/red-50.

---

## Component Inventory

### New Components

| Component | File | Purpose |
|-----------|------|---------|
| `QuizQuestion` | `src/components/quiz/QuizQuestion.tsx` | Single question with 4 tappable option cards, instant feedback |
| `QuizResults` | `src/components/quiz/QuizResults.tsx` | Score display + full question review list |
| `QuizProgress` | `src/components/quiz/QuizProgress.tsx` | Dot-based progress indicator (1/5) |
| `FlashCard` | `src/components/flashcard/FlashCard.tsx` | 3D flip card with front/back |
| `FlashCardDeck` | `src/components/flashcard/FlashCardDeck.tsx` | Embla carousel wrapper + rating buttons |
| `BookLearningProgress` | `src/components/BookLearningProgress.tsx` | Small combined progress bar for BookPage |
| `LearningPage` | `src/pages/LearningPage.tsx` | Combined quiz + flashcard page with Tabs |

### Existing Components Reused

| Component | How Used |
|-----------|----------|
| `Tabs / TabsList / TabsTrigger / TabsContent` | Quiz vs Flashcard mode on LearningPage |
| `Button` | CTAs, navigation, self-rating buttons |
| `Carousel / CarouselContent / CarouselItem` | Flashcard swipe navigation |
| `Progress` | BookPage learning progress bar |
| `RadioGroup / RadioGroupItem` | Admin correct answer selector |
| `Input` | Admin quiz option text fields |
| `Textarea` | Admin flashcard front/back text |

---

## Interaction Contracts

### Quiz Flow

| State | What User Sees | Interaction |
|-------|---------------|-------------|
| Ready | "Start Quiz" button, question count badge | Tap "Start Quiz" to begin |
| In-progress | One question with 4 option cards, progress dots | Tap option to select answer |
| Feedback (1.2s) | Selected answer highlighted correct/wrong, correct answer revealed | No interaction, auto-advance after 1200ms |
| Completed | Score display (e.g. "4/5"), full review of all questions | "Retake" button or "Back" to return |

Progress indicator: Row of dots, filled dot = answered, outlined dot = remaining, current dot = accent-colored ring.

### Flashcard Flip

| State | What User Sees | Interaction |
|-------|---------------|-------------|
| Front | Question/concept text centered on card surface | Tap anywhere on card to flip |
| Back | Answer/explanation on sage-gradient card | Tap to flip back, OR tap rating buttons |
| Rating | Two buttons below card: "Knew it" (sage) / "Didn't know" (muted) | Tap to rate, auto-advance to next card |

Flip animation: CSS `transform: rotateY(180deg)` with `perspective: 1000px`, `transform-style: preserve-3d`, `backface-visibility: hidden`. Duration: 500ms. Easing: default (ease).

Carousel navigation: Swipe left/right to change card. Embla handles tap-vs-swipe distinction (short tap = flip, drag = swipe). Previous/Next buttons below carousel for accessibility.

### Entry Points

| Location | Element | Copy | Behavior |
|----------|---------|------|----------|
| BookPage | Primary button | "Пройти тест" | Navigate to `/book/:id/learn` |
| BookPage | Progress bar | Small bar below "Пройти тест" | Shows combined quiz+flashcard % |
| ReaderPage (end) | CTA card | "Проверьте знания" | Navigate to `/book/:id/learn` |

### Admin Creation

| Element | Interaction |
|---------|-------------|
| 4th tab in AdminBookForm | "Квиз и карточки" tab label |
| Quiz section | "Add question" button, text input for question, 4 text inputs for options, radio to mark correct |
| Flashcard section | "Add card" button, textarea for front, textarea for back |
| Reorder | ChevronUp/ChevronDown buttons per item (existing pattern) |
| Delete | Trash icon per question/card with confirmation |

---

## Copywriting Contract

All copy in Russian (project language).

| Element | Copy |
|---------|------|
| Primary CTA (BookPage) | "Пройти тест" |
| Primary CTA (Learning ready) | "Начать" |
| Flashcard tab label | "Карточки" |
| Quiz tab label | "Квиз" |
| Admin tab label | "Квиз и карточки" |
| Empty state heading (no quiz) | "Квиз пока не добавлен" |
| Empty state body (no quiz) | "Квиз появится здесь, когда автор добавит вопросы к этой книге" |
| Empty state heading (no flashcards) | "Карточек пока нет" |
| Empty state body (no flashcards) | "Карточки появятся здесь, когда автор добавит их к этой книге" |
| Quiz feedback correct | "Верно!" |
| Quiz feedback wrong | "Неверно" |
| Results heading | "Результат" |
| Results score format | "{score} из {total}" |
| Retake button | "Пройти заново" |
| Back button | "Назад к книге" |
| Flashcard rating positive | "Знал" |
| Flashcard rating negative | "Не знал" |
| Flashcard progress label | "{mastered} из {total} изучено" |
| Reader CTA heading | "Проверьте знания" |
| Reader CTA body | "Пройдите тест и закрепите прочитанное" |
| Error state | "Не удалось загрузить данные. Попробуйте обновить страницу" |
| Stat card: quizzes | "Квизы" (icon: Target) |
| Stat card: flashcards | "Карточки" (icon: Brain) |
| Admin add question | "Добавить вопрос" |
| Admin add flashcard | "Добавить карточку" |
| Delete confirmation | "Удалить вопрос?": "Это действие нельзя отменить" |

---

## Layout Specifications

### LearningPage

```
[Header: Back arrow + Book title]
[Tabs: Квиз | Карточки]
─────────────────────────────
Tab Content Area:

  Quiz mode:
    [Progress dots: ● ● ○ ○ ○]
    [Question text]
    [Option A card]  ← full-width, rounded-xl, p-4
    [Option B card]
    [Option C card]
    [Option D card]

  Flashcard mode:
    [Card counter: 3 / 8]
    [Flashcard in Embla carousel]  ← h-64, rounded-2xl
    [Rating buttons: Знал | Не знал]
    [Prev / Next navigation]
    [Progress: 5 из 8 изучено]
```

### Quiz Results Screen

```
[Score display: 4 из 5]  ← text-2xl extrabold
[Progress bar: 80%]
─────────────────────────────
[Question 1 — Верно ✓]
  Your answer: [highlighted green]
[Question 2 — Неверно ✗]
  Your answer: [highlighted red]
  Correct: [highlighted green]
...
─────────────────────────────
[Пройти заново]  ← primary button
[Назад к книге]  ← ghost button
```

### BookPage Progress Bar

```
[Пройти тест]  ← primary button, full-width
[thin progress bar below]  ← h-1.5, rounded-full, sage fill
```

Progress bar: uses shadcn `Progress` component. Fill color: sage green (`bg-sage` / accent). Track: `bg-secondary`. Height: 6px (h-1.5). Border radius: full.

### ProfilePage Stats (updated)

From `grid grid-cols-3` to `flex gap-3 overflow-x-auto scrollbar-hide` to accommodate 5 cards. Each card: `min-w-[100px] rounded-2xl bg-card p-4 shadow-card`. Negative margin trick: `-mx-4 px-4` for edge-to-edge scroll with padded content.

---

## Animation Specifications

| Animation | Property | Duration | Easing | Trigger |
|-----------|----------|----------|--------|---------|
| Card flip | transform: rotateY(180deg) | 500ms | ease | Tap on card |
| Answer feedback fade | opacity + background-color | 300ms | ease-out | After answer selection |
| Progress dot fill | background-color | 200ms | ease | After advancing to next question |
| Score reveal | opacity + transform: translateY | 400ms | cubic-bezier(0.32, 0.72, 0, 1) | Results screen mount |
| Auto-advance delay | -- | 1200ms | -- | After feedback shown, before next question |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Tabs, Button, Progress, RadioGroup, Input, Textarea, Carousel, Card | not required (already installed) |

No third-party registries. No new shadcn components to install -- all already present in `src/components/ui/`.

---

## Accessibility Notes

- Quiz options: keyboard-navigable via arrow keys (rendered as button/radio pattern)
- Flashcard flip: `role="button"`, `aria-label="Flip card"`, keyboard trigger on Enter/Space
- Progress dots: `aria-label="Question {n} of {total}"`
- Rating buttons: distinct focus styles, not just color differentiation
- Tab navigation: handled by Radix Tabs (ARIA-compliant out of the box)
- Color contrast: correct/wrong feedback uses both color AND icon (Check/X) -- not color-alone

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
