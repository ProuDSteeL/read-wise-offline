---
phase: 02-reader-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/reader/SelectionToolbar.tsx
  - src/pages/ReaderPage.tsx
autonomous: true
requirements:
  - READ-04
  - READ-05
  - READ-06
  - READ-07
  - READ-08
  - READ-09
  - READ-10

must_haves:
  truths:
    - "User can select text in the reader and see a Copy button in the toolbar"
    - "Tapping Copy copies the selected text to clipboard and shows a confirmation toast"
    - "Font size, reading theme, and line spacing settings persist across page reloads via localStorage"
    - "Table of contents sidebar opens and tapping a heading scrolls to that section"
    - "User can highlight text and save quotes to their collection"
    - "Audio player can be toggled in the reader view"
  artifacts:
    - path: "src/components/reader/SelectionToolbar.tsx"
      provides: "Selection toolbar with Copy and Quote buttons"
      contains: "navigator.clipboard.writeText"
    - path: "src/pages/ReaderPage.tsx"
      provides: "Reader page passing text prop to SelectionToolbar"
      contains: "selection.text"
  key_links:
    - from: "src/pages/ReaderPage.tsx"
      to: "src/components/reader/SelectionToolbar.tsx"
      via: "text={selection.text} prop"
      pattern: "text=.*selection\\.text"
    - from: "src/components/reader/SelectionToolbar.tsx"
      to: "navigator.clipboard"
      via: "writeText call in click handler"
      pattern: "navigator\\.clipboard\\.writeText"
---

<objective>
Complete Phase 2 Reader Enhancement by adding the one missing feature (copy-to-clipboard) and verifying all 7 requirements are met.

Purpose: Research confirmed 6 of 7 requirements (READ-04 through READ-09) are already fully implemented. Only READ-10 (copy to clipboard) is partially implemented -- the SelectionToolbar has a Quote button but no Copy button. This plan adds the Copy button and verifies everything works.

Output: Updated SelectionToolbar.tsx with Copy button, updated ReaderPage.tsx to pass text prop, all 7 requirements verified.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reader-enhancement/02-RESEARCH.md

@src/components/reader/SelectionToolbar.tsx
@src/pages/ReaderPage.tsx
@src/hooks/useNativeSelection.ts

<interfaces>
<!-- Current SelectionToolbar interface (to be extended with `text` prop) -->
From src/components/reader/SelectionToolbar.tsx:
```typescript
interface Props {
  rect: DOMRect;
  onQuote: () => void;
}
export default function SelectionToolbar({ rect, onQuote }: Props)
```

<!-- Selection hook return type -->
From src/hooks/useNativeSelection.ts:
```typescript
export interface SelectionInfo {
  text: string;
  rect: DOMRect;
}
export function useNativeSelection(containerRef, enabled): { selection: SelectionInfo | null, clearSelection: () => void }
```

<!-- Current usage in ReaderPage (line 581-586) -->
```typescript
{selection && (
  <SelectionToolbar
    rect={selection.rect}
    onQuote={handleQuote}
  />
)}
```

<!-- Toast import pattern -->
```typescript
import { toast } from "@/hooks/use-toast";
toast({ title: "Цитата сохранена" });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add copy-to-clipboard button to SelectionToolbar</name>
  <files>src/components/reader/SelectionToolbar.tsx, src/pages/ReaderPage.tsx</files>
  <action>
**SelectionToolbar.tsx changes:**
1. Add `Copy` icon import from lucide-react (alongside existing `Quote` import)
2. Add `text: string` to the Props interface
3. Add `text` to the destructured props
4. Increase `BTN_W` from 120 to 240 to accommodate two buttons side by side
5. Create `handleCopy` async function that:
   - Calls `await navigator.clipboard.writeText(text)` inside try/catch
   - On success: calls `toast({ title: "Скопировано" })`
   - On error: calls `toast({ title: "Не удалось скопировать", variant: "destructive" })`
6. Import `toast` from `@/hooks/use-toast`
7. Change the toolbar layout from a single button to a flex container with two buttons:
   - Keep existing Quote button (onQuote handler, Quote icon, "Цитата" label)
   - Add new Copy button (handleCopy handler, Copy icon, "Копировать" label)
   - Both buttons should have identical styling. Use a shared flex row with `gap-1` and a divider between them
   - Wrap both buttons in the existing rounded-xl border container (move border/shadow/backdrop styles to the wrapper div)

**ReaderPage.tsx changes:**
1. In the SelectionToolbar JSX (around line 582), add `text={selection.text}` prop:
   ```
   <SelectionToolbar
     rect={selection.rect}
     text={selection.text}
     onQuote={handleQuote}
   />
   ```

**Important:** Call clipboard API directly from click handler (user gesture) to satisfy Safari/iOS security requirements. Do NOT re-read window.getSelection() -- use the `text` prop which was already captured by useNativeSelection.
  </action>
  <verify>
    <automated>cd /root/read-wise-offline && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>SelectionToolbar shows both "Цитата" and "Копировать" buttons when text is selected. Clicking "Копировать" copies text to clipboard and shows toast. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Verify all Phase 2 requirements are met</name>
  <files>src/pages/ReaderPage.tsx, src/components/reader/SelectionToolbar.tsx, src/hooks/useNativeSelection.ts, src/index.css</files>
  <action>
Perform a verification sweep of all 7 requirements by reading the relevant code and confirming each is implemented. Do NOT modify any files unless a defect is found.

**READ-04 (Font size persists):** Verify `FONT_SIZES` array exists in ReaderPage, `localStorage.setItem("reader-font-size", ...)` is called in the useEffect, and initial state reads from `localStorage.getItem("reader-font-size")`.

**READ-05 (Reading theme):** Verify `ReaderTheme` type includes "light" | "dark" | "sepia", theme buttons render all three options, `localStorage.setItem("reader-theme", ...)` persists, and `themeClasses` maps to CSS classes. Verify `src/index.css` has `.sepia` CSS variables.

**READ-06 (Line spacing):** Verify `LINE_HEIGHTS` array exists, `localStorage.setItem("reader-line-height", ...)` is called, and `lineHeight` is applied via inline style on the article element.

**READ-07 (TOC navigation):** Verify `extractToc()` parses h1-h3 headings, `slugify()` generates IDs, headings in markdown render with matching `id` attributes, TOC Sheet renders entries, and clicking an entry calls `scrollIntoView`.

**READ-08 (Highlights/quotes):** Verify `useNativeSelection` detects selection, `SelectionToolbar` appears with Quote button, `createHighlight` mutation saves to `user_highlights`, highlights render inline via `applyHighlights`, and highlight list with delete appears at article bottom.

**READ-09 (Audio in reader):** Verify headphones icon toggles `showAudioPlayer`, `MiniAudioPlayer` renders when active, and audio context integration works (play/stop on toggle).

**READ-10 (Copy to clipboard):** Verify the Copy button added in Task 1 exists in SelectionToolbar, `text` prop is passed from ReaderPage, and `navigator.clipboard.writeText` is called.

Report findings. If all pass, the phase requirements are fully met.
  </action>
  <verify>
    <automated>cd /root/read-wise-offline && grep -q "navigator.clipboard.writeText" src/components/reader/SelectionToolbar.tsx && grep -q "text={selection.text}" src/pages/ReaderPage.tsx && grep -q "reader-font-size" src/pages/ReaderPage.tsx && grep -q "reader-theme" src/pages/ReaderPage.tsx && grep -q "reader-line-height" src/pages/ReaderPage.tsx && grep -q "extractToc" src/pages/ReaderPage.tsx && grep -q "scrollIntoView" src/pages/ReaderPage.tsx && grep -q "user_highlights" src/pages/ReaderPage.tsx && grep -q "MiniAudioPlayer" src/pages/ReaderPage.tsx && echo "ALL 7 REQUIREMENTS VERIFIED" || echo "VERIFICATION FAILED"</automated>
  </verify>
  <done>All 7 requirements (READ-04 through READ-10) confirmed implemented with correct persistence, navigation, and interaction patterns. No gaps remain.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit` passes
2. All 7 requirement markers present in codebase (grep verification in Task 2)
3. SelectionToolbar has both Quote and Copy buttons
4. ReaderPage passes `text={selection.text}` to SelectionToolbar
</verification>

<success_criteria>
- SelectionToolbar renders Copy button alongside Quote button
- Clicking Copy calls navigator.clipboard.writeText with the selected text
- Toast confirms copy success or shows error on failure
- All 7 requirements (READ-04 through READ-10) verified as implemented
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-reader-enhancement/02-01-SUMMARY.md`
</output>
